# project.env is shell code, which is trusted by the framework.
# It is intended to define the basic variables and functions used by DDF, and should be
# customized for any project.

PROJECT=docker-dev-framework
ORGANIZATION=bob
SERVER_USER=bob
DOCKER_ROOT_IMAGE=ubuntu:noble
